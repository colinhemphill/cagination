/* Requirements */
require('mongoose');

/**
 * A test to get us started.
 *
 * @param  {String} input
 * @return {String}
 */
module.exports = {
  test: function(input) {
    var output = input + '!';
    return output;
  }
};
